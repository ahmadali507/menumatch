import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Admin",
  description: "Creates a Restaurant Admin account for a specific restaurant."
}

export { default } from "@/components/forms/create-admin";